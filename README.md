### Race

A WIP racing game

#### Building

- Install the [LunarG Vulkan SDK](https://vulkan.lunarg.com/) or `libvulkan-dev` package
- Install CMake

```bash
git submodule init
git submodule update
mkdir build
cd build
cmake ..
cmake --build .
```
